namespace OpenShaderGraph.Core.View.UI.Sidebar.MenuBar
{
    public static class MenuEnums
    {
        // Central place to keep enum definitions shared by multiple UI scripts.
        // Additional enums (EditMenuItem, ViewMenuItem, etc.) can be added here in the future.

        public enum FileMenuItem
        {
            NewGraph,
            OpenGraph,
            Save,
            SaveAs,
            Export
        }
    }
}