# nodes/inputs/Normal.yml
name: "Normal"
category: "inputs"
description: "Provides access to the mesh's normal vectors."

template_code: "{{out}} = NORMAL;"

inputs: []

outputs:
  - name: "out"
    type: "vec3"
