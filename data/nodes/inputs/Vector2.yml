# nodes/inputs/Vector2.yml
name: "Vector2"
category: "inputs"
description: "Provides a 2D vector value."

template_code: "{{out}} = vec2({{value.x}}, {{value.y}});"

inputs:
  - name: "value"
    type: "vec2"
    default_value: { x: 0.0, y: 0.0 }

outputs:
  - name: "out"
    type: "vec2"
