# nodes/inputs/UV.yml
name: "UV"
category: "inputs"
description: "Provides access to the mesh's UV coordinates."

template_code: "{{out}} = UV;"

inputs: []

outputs:
  - name: "out"
    type: "vec2"
