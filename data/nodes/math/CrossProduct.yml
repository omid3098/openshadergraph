# nodes/math/CrossProduct.yml
name: "Cross Product"
category: "math"
description: "Outputs the cross product of two 3D vectors."

template_code: "{{out}} = cross({{a}}, {{b}});"

inputs:
  - name: "a"
    type: "vec3"
  - name: "b"
    type: "vec3"

outputs:
  - name: "out"
    type: "vec3"
