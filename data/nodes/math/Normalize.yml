# nodes/math/Normalize.yml
name: "Normalize"
category: "math"
description: "Outputs a normalized vector."

template_code: "{{out}} = normalize({{in}});"

inputs:
  - name: "in"
    type: "void"

outputs:
  - name: "out"
    type: "void"
