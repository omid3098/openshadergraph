# nodes/math/DotProduct.yml
name: "Dot Product"
category: "math"
description: "Outputs the dot product of two vectors."

template_code: "{{out}} = dot({{a}}, {{b}});"

inputs:
  - name: "a"
    type: "void"
  - name: "b"
    type: "void"

outputs:
  - name: "out"
    type: "float"
