# nodes/core/Input.yml
name: "Input"
category: "core"
description: "Defines a single input pin for a sub-graph."

# This node has no code, it's a structural marker.

# It has no inputs of its own.
inputs: []

# Its output is dynamically typed based on user configuration.
outputs:
  - name: "out"
    type: "void" # The type will be set by the user in the editor.
