# nodes/core/Output.yml
name: "Output"
category: "core"
description: "Defines a single output pin for a sub-graph."

# This node has no code, it's a structural marker.

# Its input is dynamically typed based on user configuration.
inputs:
  - name: "in"
    type: "void" # The type will be set by the user in the editor.

# It has no outputs of its own.
outputs: []
